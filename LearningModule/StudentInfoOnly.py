class StudenInfoOnly:
    def __int__(self):
        pass




#If file contains only the classes, then file should class name only.
#For best practice, we should maintain a single class in the single file