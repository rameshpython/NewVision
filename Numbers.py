
class Numbers:
    def __init__(self):
        pass

    def calculate(self,a,b,c):
        total = a + b + c
        product = a * b * c
        average = total/3
        return f"sum = {total},product ={product},average ={average}"
        #f "  " f is indicates only for string format in curly brackets
        #we assign value for that variable.
